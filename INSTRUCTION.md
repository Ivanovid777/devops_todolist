# Repository
https://hub.docker.com/repository/docker/ivanoid777/mate_repo/general

# to launch the app just run container by following command:
# docker run -d -p <host_port>:8080 ivanoid777/mate_repo:1.0

# to use this app open browser via [http://127.0.0.1:<host_port>/]()